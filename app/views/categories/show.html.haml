%h3= @category.name

= link_to 'New Thread', new_category_forum_thread_path(@category), class: 'button'

- @category.threads.each do |thread|
	= render partial: 'thread_row', locals: { thread: thread }